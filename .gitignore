/target
/Cargo.lock

# Top-level wit/deps fetched by wkg (not committed; use just init-wit-deps locally)
wit/deps/
# component wit/deps/ ARE committed so CI can build without wkg

# Generated WIT bindings
**/src/bindings.rs

# wasmCloud build artifacts
components/*/build/
*.wasm

# Config secrets
config/*/secrets.env
.env

# OS
.DS_Store


# Ruvector

**ruvector**